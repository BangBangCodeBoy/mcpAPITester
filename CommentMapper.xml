<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.codeboy.mvc.model.dao.CommentDao">

	<select id="selectAllByUserProblemSetId" resultType="Comment">
		SELECT *
		FROM comment
		WHERE user_problem_id = #{userProblemSetId}
	</select>

	<insert id="insertComment">
		INSERT INTO comment (member_id, content, comment_date, user_problem_set)
		VALUES (#{member_id}, #{content}, #{comment_date, #{user_problem_set})
	</insert>

	<update id="updateComment">
		UPDATE comment
		SET content = #{content}
		WHERE comment_id = #{commentId}
	</update>

	<delete id="deleteComment">
		DELETE
		FROM comment
		WHERE comment_id = #{commentId}
	</delete>

    <select id="">

    </select>
</mapper>
